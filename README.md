# roadsidebill
Website for roadsidebill.com

## Static site

A minimal static HTML site has been added for local preview and deployment.

- Preview locally: `python3 -m http.server` then open http://localhost:8000
- To publish: enable GitHub Pages on the `main` branch or use an actions workflow.

